# Cursor User Rule (fallback)

If the hook is configured, you usually do not need this. If the hook does not fire, paste the block below into **Cursor Settings → Rules → User Rules**:

```markdown
When the user learns technical concepts, reviews topics, prepares for interviews, navigates knowledge (Zoom In/Out, Navigate), or asks how two concepts relate, you MUST Read and follow ~/.cursor/skills/kan/SKILL.md and SPEC/ + EXAMPLES/ as needed. Phase 0 atlas first (ASCII map, not a pure tree); never lead with prose walls. Use English. For codebase/repo learning, use project-learning-coach instead.
```
