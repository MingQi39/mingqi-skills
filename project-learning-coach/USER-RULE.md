# Cursor User Rule (backup)

Usually unnecessary if your hook is already active.  
If the hook does not apply, copy this block into **Cursor Settings -> Rules -> User Rules**:

```markdown
When the user asks how to learn a repository, understand architecture, trace a request path, or asks "what should I learn today", always load and follow ~/.cursor/skills/project-learning-coach/SKILL.md and its references directory first. If the project includes AI/Agent/LLM elements, also load ai-platform-coach. Follow mode-based interaction defined in the skill, and respond in Simplified Chinese.
```
