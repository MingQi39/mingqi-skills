# Migrated

`interview-driven-learning` has been upgraded to **KAN v3.0** (Knowledge Atlas Navigator).

Use the [`../kan/`](../kan/) directory.

Install:

```bash
ln -sfn "$(pwd)/kan" ~/.cursor/skills/kan
```
